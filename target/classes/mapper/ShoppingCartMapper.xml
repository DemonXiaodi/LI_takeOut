<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="priv.anna.reggie.mapper.ShoppingCartMapper">


    <select id="findExistsInShoppingCart" resultType="priv.anna.reggie.domain.ShoppingCart">
        SELECT * FROM shopping_cart WHERE
        (dish_id = #{dishId} OR setmeal_id=#{setmealId})
        AND user_id = #{userId}

    </select>



    <insert id="add" parameterType="priv.anna.reggie.domain.ShoppingCart">
        insert into shopping_cart values(#{id},#{name},#{image},#{userId},#{dishId},
        #{setmealId},#{dishFlavor},#{number},#{amount},#{createTime})
    </insert>


    <delete id="delete" parameterType="priv.anna.reggie.domain.ShoppingCart">
        delete from shopping_cart where
        (dish_id = #{dishId} OR setmeal_id=#{setmealId})
        AND user_id = #{userId}
    </delete>

    <update id="update" parameterType="priv.anna.reggie.domain.ShoppingCart">
        update shopping_cart
        <set>
            <if test="name!=null">
                name = #{name},
            </if>
            <if test="image!=null">
                image = #{image},
            </if>
            <if test="user_id!=null">
                user_id = #{userId},
            </if>
            <if test="dish_id!=null">
                dish_id = #{dishId},
            </if>
            <if test="setmeal_id!=null">
                setmeal_id = #{setmealId},
            </if>
            <if test="dish_flavor!=null">
                dish_flavor = #{dishFlavor},
            </if>
            <if test="number!=null">
                number = #{number},
            </if>
            <if test="amount!=null">
                amount = #{amount},
            </if>
            <if test="createTime!=null">
                create_time = #{createTime},
            </if>
        </set>
        where id=#{id}
    </update>

    <update id="updateNumber" parameterType="priv.anna.reggie.domain.ShoppingCart">
        update shopping_cart set number = #{number} where id=#{id}
    </update>

    <select id="findByUserId" resultType="priv.anna.reggie.domain.ShoppingCart">
        select * from shopping_cart where user_id = #{userId} ORDER BY create_time DESC
    </select>

    <delete id="deleteByUserId" parameterType="long">
        delete from shopping_cart WHERE user_id = #{userId}
    </delete>



</mapper>